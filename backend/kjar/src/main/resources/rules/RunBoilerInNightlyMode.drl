package rules

import com.ftn.sbnz.model.Home
import com.ftn.sbnz.model.Boiler

rule "run boiler in nightly mode"
    when
        $home: Home(consumptionProfile == consumptionProfile.NIGHTLY)
        $boiler: Boiler()
    then
        modify($boiler){setOn(true)}
end
